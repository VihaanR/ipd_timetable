# --- solver engine ---
ortools>=9.9            # MIP (CBC) + CP-SAT backends
pandas>=2.0             # export tables / benchmark CSV
openpyxl>=3.1           # .xlsx export
fpdf2>=2.7              # .pdf export

# --- web platform ---
fastapi>=0.111          # API framework
uvicorn>=0.30           # ASGI server (run on port 8750; 8000 is OS-reserved on the dev box)
python-multipart>=0.0.9 # multipart parsing for calendar file upload (pdf/jpg/png)
sqlmodel>=0.0.16        # SQLite persistence (SQLAlchemy+Pydantic); Postgres-portable for cloud

# --- calendar ingestion ---
pillow>=10.0            # validate/downscale uploaded jpg/png before storage or AI extraction
pypdf>=4.0              # validate PDFs, page count, free text-layer extraction (born-digital)
anthropic>=0.40         # optional: Claude-vision holiday extraction; feature hides without ANTHROPIC_API_KEY

# --- tests ---
pytest>=8.0             # test runner
httpx>=0.27             # required by fastapi.testclient for API endpoint tests
