__pycache__/
*.pyc
.pytest_cache/
benchmarks/results/*.csv
*.xlsx
*.pdf
!references/*.pdf
.venv/

# Platform runtime data (the only code-written locations — must not be committed)
webapp/data/
webapp/uploads/

# Subagent-driven-development scratch (git-ignored ledger/scratch)
.superpowers/

# Transient runtime lock
.claude/scheduled_tasks.lock
